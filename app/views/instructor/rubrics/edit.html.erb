<p><%= link_to 'Back to all rubrics', :controller => '/instructor/rubrics', :course => @course, :assignment => @assignment %></p>

<div class="SectionHeader"><div class="Full">
<h2>Edit Rubric for '<%=h @assignment.title%>' </h2>
</div></div>

<div id="form_area">
  <%= error_messages_for 'rubric' %>	
  <% form_tag( :action => 'update', :course => @course, :assignment => @assignment, :id => @rubric ) do %>

  <%= render :partial => 'form' %>
 
  <div class="submit">
	<%= submit_tag "Update Rubric" %>
  </div>
  <% end %>	
</div>
