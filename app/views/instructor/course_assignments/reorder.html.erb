<h2>Reorder Assignments for '<%= @course.title %>'</h2>

<div id="reorder">
  <ul id="assignment-order">
    <% for assignment in @course.assignments %>
      <li id="assignment_<%= assignment.id %>"><i class="icon-random"></i>
        <b><%= assignment.title %>  -  (Due: <%= assignment.due_date.to_formatted_s(:compact_date) %>)</b>
      </li>
    <% end %>
  </ul>

  <%= link_to "I'm done reordering assignments.", {:controller => '/instructor/course_assignments', :course => @course}, {:class => 'btn btn-primary'} %>
</div>

<%= sortable_element 'assignment-order', 
    :url => { :action => 'sort', :course => @course },
    :complete => visual_effect(:highlight, 'assignment-order')
%>