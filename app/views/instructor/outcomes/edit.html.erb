<p><%= link_to 'Back', :controller => '/instructor/outcomes', :action => nil, :course => @course %></p>

<div class="SectionHeader"><div class="Full">
<h2>Edit outcome for '<%=h @course.title%>' </h2>
</div></div>

<p class="blockintro">If you change the parent outcome for this outcome, all positions and numberings will be recalculated.   All outcomes that are children of this outcome will be moved as well.</p>

<div id="form_area">
  <%= error_messages_for 'course_outcome' %>	
  <% form_tag( :action => 'update_outcome', :course => @course, :id => @course_outcome.id ) do %>

  <%= render :partial => 'outcome_form' %>
 
  <div class="submit">
	<%= submit_tag "Save Changes to Outcome" %>
  </div>
  <% end %>	
</div>