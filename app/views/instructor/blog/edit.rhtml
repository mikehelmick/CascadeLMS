<div id="form_area">
<h2>Editing blog post for <%= @course.title %></h2>
<p><%= link_to 'Cancel', :action => 'index' %></p>

<%= start_form_tag :action => 'update', :id => @post %>
  <%= render :partial => 'form' %>
<div class="submit">
  <%= submit_tag 'Edit' %>
</div>
<%= end_form_tag %>

</div>

<%= link_to 'Cancel', :action => 'index' %>
