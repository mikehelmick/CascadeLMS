<h2>Program Managers/Auditors for '<%= @program.title %>'</h2>

<p><%= link_to 'Back', :action => nil, :id => @program %></p>

<h3>Program Managers</h3>

<div id="managers_list">
<% if @managers.length == 0 %>
  <p><i>No program managers</i></p>
<% else %>
  <ul>
	<% for user in @managers %>	
	   <li id="manager_user_<%=user.id%>">
		<%= link_to_remote( image_tag('delete.png', :align => 'absmiddle' ), :url => { :action => 'deluser', :type => 'manager', :id => @program.id, :user => user }, :confirm => "Remove user #{user.display_name} from this program?", :success => "new Effect.Puff( 'manager_user_#{user.id}' )", :failure => "alert('There was an error removing the selected user from this program.');" ) %> <%=h user.display_name %> - <b><%=h user.uniqueid %></b> - (<%=h user.email%>)
	   </li>		
	<% end %>
  </ul>
<% end %>
</div>

<hr/>
<h3>Program Auditors</h3>

<div id="auditors_list">
<% if @auditors.length == 0 %>
  <p><i>No program auditors</i></p>
<% else %>
  <ul>
	<% for user in @auditors %>
	  <li id="auditor_user_<%=user.id%>">
		<%= link_to_remote( image_tag('delete.png', :align => 'absmiddle' ), :url => { :action => 'deluser', :type => 'auditor', :id => @program.id, :user => user }, :confirm => "Remove user #{user.display_name} from this program?", :success => "new Effect.Puff( 'auditor_user_#{user.id}' )", :failure => "alert('There was an error removing the selected user from this program.');" ) %> <%=h user.display_name %> - <b><%=h user.uniqueid %></b> - (<%=h user.email%>)
		</li>
	<% end %>
  </ul>
<% end %>
</div>
