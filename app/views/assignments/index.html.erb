
<h2>Current Assignments for <%= @course.title %></h2>
<%= render :partial => 'assignment_list', :locals => { :assignments => @current_assignments, :public => false } %>

<h2>Upcoming Assignments for <%= @course.title %></h2>
<%= render :partial => 'assignment_list', :locals => { :assignments => @upcoming_assignments, :public => false } %>

<h2>Past Due Assignments for <%= @course.title %></h2>
<%= render :partial => 'assignment_list', :locals => { :assignments => @complete_assignments, :public => false } %>
